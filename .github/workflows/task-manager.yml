name: Менеджер задач

on:
  workflow_dispatch:
    inputs:
      action:
        description: Действие
        required: true
        type: choice
        options:
          - добавить
          - список
          - статистика
      task:
        description: 'Название задачи'
        required: false
        type: string
      category:
        description: 'Категория'
        required: false
        type: choice
        options:
          - учеба
          - личное
          - работа
      priority:
        description: 'Приоритет'
        required: false
        type: choice
        options:
          - высокий
          - средний
          - низкий

jobs:
  manage-tasks:
    runs-on: ubuntu-latest
    steps:
      - name: Загружаем код
        uses: actions/checkout@v4

      - name: Обрабатываем действие
        id: process_action
        run: |
          ACTION="${{ github.event.inputs.action }}"
          TASK="${{ github.event.inputs.task }}"
          
          if [ ! -f "tasks.json" ]; then
            echo '{"tasks": [], "last_update": "", "total_completed": 0}' > tasks.json
          fi
          
          case $ACTION in
            "добавить")
              if [ -z "$TASK" ]; then
                echo "message=❌ Не указана задача для добавления" >> $GITHUB_OUTPUT
                exit 0
              fi
              
              TASK_ID="task_$(date +%s)"
              CATEGORY="${{ github.event.inputs.category }}"
              PRIORITY="${{ github.event.inputs.priority }}"
              
              if [ -z "$CATEGORY" ]; then
                CATEGORY="общее"
              fi
              
              if [ -z "$PRIORITY" ]; then
                PRIORITY="средний"
              fi
              
              jq --arg id "$TASK_ID" \
                 --arg task "$TASK" \
                 --arg category "$CATEGORY" \
                 --arg priority "$PRIORITY" \
                 --arg created "$(date +"%Y-%m-%d %H:%M:%S")" \
                 --arg status "не начато" \
                 '.tasks += [{"id": $id, "task": $task, "category": $category, "priority": $priority, "created": $created, "status": $status}] | .last_update = $created' \
                 tasks.json > temp_tasks.json
              mv temp_tasks.json tasks.json
              
              echo "message=✅ Задача добавлена: $TASK
📂 Категория: $CATEGORY
⚡ Приоритет: $PRIORITY" >> $GITHUB_OUTPUT
              ;;
              
            "список")
              TASK_COUNT=$(jq -r '.tasks | length' tasks.json)
              
              if [ $TASK_COUNT -eq 0 ]; then
                echo "message=📭 Список задач пуст" >> $GITHUB_OUTPUT
              else
                MESSAGE="📋 СПИСОК ЗАДАЧ ($TASK_COUNT)"
                
                for i in $(seq 0 $((TASK_COUNT - 1))); do
                  TASK_NAME=$(jq -r ".tasks[$i].task" tasks.json)
                  CATEGORY=$(jq -r ".tasks[$i].category" tasks.json)
                  PRIORITY=$(jq -r ".tasks[$i].priority" tasks.json)
                  STATUS=$(jq -r ".tasks[$i].status" tasks.json)
                  
                  case $STATUS in
                    "не начато") EMOJI="🔴" ;;
                    "в процессе") EMOJI="🟡" ;;
                    "выполнено") EMOJI="🟢" ;;
                    "отложено") EMOJI="⏸️" ;;
                    *) EMOJI="📌" ;;
                  esac
                  
                  case $PRIORITY in
                    "высокий") PRIORITY_EMOJI="🔺" ;;
                    "средний") PRIORITY_EMOJI="🟡" ;;
                    "низкий") PRIORITY_EMOJI="🔻" ;;
                    *) PRIORITY_EMOJI="📌" ;;
                  esac
                  
                  MESSAGE="$MESSAGE
$EMOJI $PRIORITY_EMOJI $TASK_NAME
   📂 $CATEGORY | 📊 $STATUS"
                done
                
                echo "message=$MESSAGE" >> $GITHUB_OUTPUT
              fi
              ;;
              
            "статистика")
              TASK_COUNT=$(jq -r '.tasks | length' tasks.json)
              COMPLETED=$(jq -r '.total_completed' tasks.json)
              LAST_UPDATE=$(jq -r '.last_update' tasks.json)
              
              if [ -z "$LAST_UPDATE" ] || [ "$LAST_UPDATE" = "null" ]; then
                LAST_UPDATE="никогда"
              fi
              
              MESSAGE="📊 СТАТИСТИКА ЗАДАЧ"
              MESSAGE="$MESSAGE
• Всего задач: $TASK_COUNT
• Выполнено: $COMPLETED
• Последнее обновление: $LAST_UPDATE"
              
              if [ $TASK_COUNT -gt 0 ]; then
                COMPLETION_RATE=$((COMPLETED * 100 / TASK_COUNT))
                MESSAGE="$MESSAGE
📈 Прогресс: $COMPLETION_RATE% выполнено"
                
                if [ $COMPLETION_RATE -ge 80 ]; then
                  MESSAGE="$MESSAGE
🎉 Отличная работа! Продолжай в том же духе!"
                elif [ $COMPLETION_RATE -ge 50 ]; then
                  MESSAGE="$MESSAGE
👍 Хорошие результаты! Можно лучше!"
                else
                  MESSAGE="$MESSAGE
💪 Есть над чем поработать! Не сдавайся!"
                fi
              fi
              
              echo "message=$MESSAGE" >> $GITHUB_OUTPUT
              ;;
              
            *)
              echo "message=❌ Неизвестное действие: $ACTION" >> $GITHUB_OUTPUT
              ;;
          esac

      - name: Отправляем результат
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          message: ${{ steps.process_action.outputs.message }}

      - name: Сохраняем изменения
        if: github.event.inputs.action == 'добавить'
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "GitHub Actions"
          git add tasks.json
          git commit -m "Добавлена задача: ${{ github.event.inputs.task }}" || echo "Нет изменений"
          git push || echo "Нечего пушить"
